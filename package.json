{
  "name": "readme-radar",
  "private": true,
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "preview": "node scripts/generate-preview.mjs",
    "generate": "node scripts/fetch-and-generate.mjs"
  }
}
